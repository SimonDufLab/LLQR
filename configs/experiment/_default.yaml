# @package _global_
defaults:
  - /default        # includes default.yaml from default/
  - /dataset: mnist  # this now correctly maps to dataset/mnist.yaml
  - /architecture: mlp
  - /lr_scheduler: null

lr_scheduler: null  # TODO: introduces a mismatch when overriding with default experiment